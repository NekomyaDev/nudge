[package]
name = "nudgec"
version = "0.1.0"
description = "The Nudge compiler — a typed, replayable, budget-aware language for LLM agents"
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
